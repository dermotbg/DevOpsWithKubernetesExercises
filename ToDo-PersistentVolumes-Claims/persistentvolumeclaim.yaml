apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: todo-ns
  name: todo-claim
spec:
  storageClassName: todo-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi